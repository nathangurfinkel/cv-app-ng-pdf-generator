# CORS Configuration
CORS_ORIGINS=https://your-frontend-domain.com

# Debug Configuration
DEBUG=false
VERBOSE=false

# Frontend URL (for PDF generation - legacy, use FRONTEND_ORIGINS instead)
FRONTEND_URL=https://your-amplify-app.com

# Frontend Origins (SSRF protection - comma-separated list)
# Only these origins are allowed for frontend_url in PDF generation requests
FRONTEND_ORIGINS=https://your-amplify-app.com,https://your-frontend-domain.com
